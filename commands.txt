python -m venv .venv
.venv\Scripts\activate

pip install -r requirements.txt
uvicorn app.main:app --reload
uvicorn app.main:app --reload --reload-dir app


{
  "product_request": "Build a simple todo app"
}

Connect WS: GET /ws/projects/{project_id}/epics?token=<JWT>
Send: {"type":"epics.generate","constraints":"","count":6}



Send: {"type":"epics.regenerate","constraints":"must support SSO","count":6} (same endpoint; new run created)
Send: {"type":"epics.approve","batch_id":"<batch_id>","approve_all":true}
Send: {"type":"epics.latest"}->get epics

Server emits/streams epics.approved (and also pushes a WS response {"type":"epics.approved",...})


milestone 5

ws://localhost:8000/ws/projects/<project_id>/specs?token=<JWT>
